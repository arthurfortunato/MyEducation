export const paragraphs = [
  <p>
    O <strong>Spring</strong> é um vasto ecossistema com diversos projetos, e o
    fundamento de toda essa estrutura é o <strong>Spring Framework</strong>. Ele
    oferece suporte essencial para dois conceitos chave:{" "}
    <strong>inversão de controle (IoC)</strong> e{" "}
    <strong>injeção de dependências (DI)</strong>, permitindo o gerenciamento
    eficiente dos <strong>beans</strong> (objetos gerenciados pelo Spring).
  </p>,
  <p>
    A <strong>inversão de controle (IoC)</strong> é um padrão de design que
    define como as dependências de um objeto são resolvidas e gerenciadas sem a
    necessidade de o próprio objeto criar ou manter essas dependências. Esse
    papel é delegado ao framework, que, no caso do <strong>Spring</strong>, é o
    seu <strong>Core</strong>.
  </p>,
  <p>
    O <strong>Spring Core</strong> é responsável por gerenciar as dependências
    dos objetos conforme necessário, e faz isso utilizando o conceito de{" "}
    <strong>injeção de dependências (DI)</strong>.
  </p>,
  <p>
    A <strong>injeção de dependências</strong> é a implementação concreta da
    <strong> inversão de controle</strong>, permitindo que o{" "}
    <strong>Spring</strong> crie e gerencie automaticamente os{" "}
    <strong>beans</strong>, que são instâncias dos objetos usados em uma
    aplicação.
  </p>,
  <p>
    Toda essa base de funcionalidades do <strong>Spring</strong> está contida no
    <strong> Spring Core</strong>, que faz parte do projeto{" "}
    <strong>Spring Framework</strong>. O Core traz várias configurações padrão
    prontas para uso, mas também oferece flexibilidade para que os
    desenvolvedores personalizem e criem as configurações específicas de acordo
    com as necessidades da aplicação.
  </p>,
  <p>
    Inicialmente, o <strong>Spring</strong> utilizava arquivos{" "}
    <strong>XML</strong> para configurar essas dependências e o comportamento da
    aplicação. No entanto, atualmente, a tendência é cada vez mais utilizar
    anotações <strong>(@Annotations)</strong>, que oferecem uma maneira mais
    simples e legível de configurar o framework.
  </p>,
];
