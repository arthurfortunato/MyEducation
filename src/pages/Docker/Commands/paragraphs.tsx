export const paragraphs = [
  <p>
    <strong>docker run</strong>: Cria e inicia um contêiner a partir de uma
    imagem. Por exemplo: docker run nome_da_imagem.
  </p>,
  <p>
    <strong>docker build</strong>: Constrói uma imagem a partir de um
    Dockerfile. Por exemplo: docker build -t nome_da_imagem . (o ponto indica o
    diretório atual).
  </p>,
  <p>
    <strong>docker images</strong>: Lista todas as imagens disponíveis no
    sistema Docker.
  </p>,
  <p>
    <strong>docker ps</strong>: Lista todos os contêineres em execução no
    momento.
  </p>,
  <p>
    <strong>docker stop</strong>: Interrompe a execução de um contêiner em
    execução. Por exemplo: docker stop nome_do_contêiner.
  </p>,
  <p>
    <strong>docker rm</strong>: Remove um contêiner. Por exemplo: docker rm
    nome_do_contêiner.
  </p>,
  <p>
    <strong>docker rmi</strong>: Remove uma imagem do Docker. Por exemplo:
    docker rmi nome_da_imagem.
  </p>,
  <p>
    <strong>docker exec</strong>: Executa um comando dentro de um contêiner em
    execução. Por exemplo: docker exec nome_do_contêiner comando.
  </p>,
  <p>
    <strong>docker pull</strong>: Baixa uma imagem do Docker Hub. Por exemplo:
    docker pull nome_da_imagem.
  </p>,
  <p>
    <strong>docker push</strong>: Envio uma imagem para o Docker Hub ou um
    registro de contêiner privado. Por exemplo: docker push nome_da_imagem.
  </p>,
  <p>
    Esses são apenas alguns dos comandos básicos do Docker que você pode começar
    a usar. O Docker oferece muitos outros comandos e opções para gerenciar
    imagens, contêineres, redes, volumes e muito mais. Para obter informações
    mais detalhadas sobre os comandos do Docker, você pode consultar a
    documentação oficial do Docker: https://docs.docker.com/
  </p>,
];
