export const paragraphs = [
  <p>
    A <strong>Orientação a Objetos</strong> é uma abordagem para a programação
    que se baseia em conceitos como{" "}
    <strong>classes, objetos, herança, polimorfismo e encapsulamento</strong>.
    Esses conceitos são utilizados para modelar o mundo real em uma forma
    computacional, tornando a programação mais clara, organizada e reutilizável.
  </p>,

  <p>
    Em vez de lidar com dados e funções de forma separada, a{" "}
    <strong>orientação a objetos </strong> permite que você crie "objetos" que
    combinam dados e funções relacionadas.
  </p>,

  <p>
    <strong>Classe</strong>: é uma estrutura que descreve um conjunto de objetos
    com características em comum. É uma definição abstrata que contém
    informações sobre os atributos (variáveis) e métodos (funções) que os
    objetos dessa classe possuem.
  </p>,

  <p>
    <strong>Objeto</strong>: é uma instância de uma classe, ou seja, é uma
    entidade concreta criada a partir de uma definição de classe. Cada objeto
    tem seus próprios valores de atributos e pode executar seus próprios
    métodos.
  </p>,

  <p>
    <strong>Herança</strong>: é um mecanismo que permite criar uma nova classe a
    partir de uma classe já existente, herdando todos os atributos e métodos da
    classe original e adicionando novos atributos e métodos. A classe que é
    herdada é chamada de superclasse ou classe base, enquanto a nova classe é
    chamada de subclasse ou classe derivada.
  </p>,

  <p>
    <strong>Polimorfismo</strong>: é a capacidade de um objeto ser tratado como
    se fosse outro tipo de objeto. É possível usar uma mesma interface para
    representar diferentes implementações de uma mesma funcionalidade.
  </p>,

  <p>
    <strong>Encapsulamento</strong>: é o mecanismo que garante que os dados de
    um objeto só possam ser acessados através de métodos específicos, que
    controlam o acesso aos atributos do objeto. Isso garante a integridade dos
    dados e evita que outros objetos modifiquem os dados diretamente.
  </p>,
];
